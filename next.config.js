/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    images: {
        domains: ["images.example.com", "talking-with-pdf.s3.ap-south-1.amazonaws.com"],
      },
  };
